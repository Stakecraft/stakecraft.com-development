# Client-only routes are emitted as real HTML files by scripts/copy-client-routes.js
# (notadmin.html, health.html). Do NOT rewrite them to /index.html — Pages then
# redirects /index.html → / with 308, which breaks /notadmin and /health.
#
# Unknown paths keep a proper 404 for SEO (we ship public/404.html).
/*    /404.html   404
